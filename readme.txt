importer un répertoire github:
git clone "adresse du répertoire en ligne"

importer les changements:
git pull --all

exporter les changements:
git add --all      # sélectionne tous les fichiers du projet pour l'exportation
git commit -m "description des changements"
git push --all     # exporte les fichiers au répertoire
